using TensorToolbox
using Plots
include("params.jl")
include("TLD.jl")
include("newton_update.jl")

function main()
    T = normalize(rand( 500, 500, 500),1)

    T_sugi  = TLD(T)
    T_sugiN = TLD_newton(T)
end

main()
